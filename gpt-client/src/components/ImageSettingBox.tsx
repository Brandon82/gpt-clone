import React, { useContext } from 'react';
import { GPTContext } from '@state/GPTContext';


export const ImageSettingBox: React.FC<{}> = () => {
  const {state} = useContext(GPTContext);


  return (
    <div className="image-settings">
        
    </div>
  );
};
